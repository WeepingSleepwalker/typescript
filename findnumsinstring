function sumNumbers(test: string): number {
    // your code here
    let array = test.split(" ");
    let sum = 0;
    for(let i =0; i<=array.length-1; i++){
        let holder = Number(array[i]);
        if(holder){
        sum += Number(array[i]);
       }
    }
    return sum;
}
