function replaceFirst(values: number[]): number[] {
    if (values === undefined || values.length == 0) {
    // array empty or does not exist
    return values;
    }
    //take out the first number
    let first = values.shift();
    //add it to the back
    let answer = values.concat(first);
 
    return answer;
}

